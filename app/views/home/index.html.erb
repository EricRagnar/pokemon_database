<!-- First Grid -->
<div class="w3-row-padding w3-padding-64 w3-container">
 <section class="layout">
        <% @pokemon.each do |item| %>
            <div class="pokemon">
              <%=item.name%>
            </div>
        <% end %>
  </section>
  </div>
